#ifndef I_testBlock_H
#define I_testBlock_H
#include "Block.hh"
// returns true if aBlock seems to work properly, false if otherwise
// prints reason to std::cerr if false
bool testBlock( Block * aBlockp );
#endif //I_testBlock_H
